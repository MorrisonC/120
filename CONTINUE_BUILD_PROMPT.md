# Continue 120 Build Prompt

Copy and paste the prompt below into a fresh Jules AI session to resume development seamlessly on project 120.

```text
Please execute the `continue-120-build` skill to resume development on Project 120 ("Every 120 you die").

Steps to follow:
1. Run `./resume.sh` to orient on Progress.md, TASK_QUEUE.md, and SESSION_LOG.md.
2. Select the next unblocked task or gauntlet target.
3. Implement the changes following small-task discipline.
4. Verify using unit tests (GUT) and web E2E test harness (`node serve_and_test.js` or `capture_web_e2e.sh`).
5. Update TASK_QUEUE.md and Progress.md accordingly.
6. Append a session summary entry to SESSION_LOG.md following `project-120-docs/session-log-format.md`.
```
